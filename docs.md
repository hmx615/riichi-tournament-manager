# 第一版领域约定

## 统一比赛流程

`1st XRC` 和未来新比赛都执行相同流程：

1. 创建比赛并配置计划场数、原点和顺位马点。
2. 登记选手及其已知天凤用户名。
3. 创建或自动递增赛程编号。
4. 录入天凤原始牌谱或 NAGA 分析链接。
5. 从数据源读取真实昵称并按别名自动匹配选手。
6. 管理员确认或人工调整东南西北的选手归属。
7. 计算单场分数、小局统计及赛事汇总。
8. 异常数据进入待复核状态，不直接混入正式统计。

## 身份修正规则

- 原始昵称永久保留在每场的 `sourceUsername` 中。
- 默认匹配来自选手的 `usernames` 别名集合。
- 管理员可以逐场修改 `participantId`，修改后标记为 `manual`。
- 后续持久化阶段需为每次修改记录修改人、时间、旧值、新值和原因。
- 修改某场身份后，仅重算受影响比赛，不修改牌谱原始内容。

## 当前计分口径

- 原点：25000
- 顺位马点：一位 +30、二位 +10、三位 -10、四位 -30
- 单场赛事分：`(终局点数 - 原点) / 1000 + 顺位马点`
- 同点时当前实现沿用天凤座次顺序；引入其他赛事规则时应配置同点处理方式。

## 统计口径

- 副露不含暗杠。
- 双响放铳按一个小局计入放铳率，放铳事件数仍分别保留。
- 立直多面率：立直时听牌种类数至少 2 种的立直占比。逐张枚举 34 种牌判定能否和牌，因此两面、双碰、单骑、
  国士等所有牌形都按实际待牌种类数计算（双碰听 2 种即计入多面）。
- 立直好型率：立直时待牌剩余张数至少 6 张的立直占比。剩余张数按“每种待牌 4 张减去自己视角可见张数”累计，
  可见范围包含自己手牌、立直瞬间已打出的四家河底、已鸣出的副露与已翻开的宝牌指示牌。
- 里宝率：带里宝（裏ドラ 1 飜以上）的立直和了 ÷ 立直和了。天凤标准牌谱只在有里宝时列出「裏ドラ(N飜)」，
  NAGA 导出牌谱即使 0 飜也会列出，因此统一按飜数判断。
- 平均里宝数：立直和了时的平均里宝飜数（分母为立直和了数）。
- 被炸率：被炸庄满贯以上次数 ÷ 被自摸次数。分子限定“自己是庄家、被自摸、且对方合计点数 ≥ 8000（满贯）”。
- 被自摸率：被自摸的局数占统计局数的比例（保留旧口径，不在三列里展示）。
- 局收支：（终局素点合计 − 起始素点 25000 × 对局数）÷ 统计局数，即每局平均素点收支。
- 平均立直巡目：立直巡目平均值（旧字段名「立直巡目」）。
- 统计结果按“口径版本 + 数据版本”缓存在 D1；改动统计口径时需要把 `SNAPSHOT_REVISION` 加一，旧快照才会重算。
- 两个立直牌形指标只统计能从牌谱复现手牌的立直；复现失败的立直不进入分母，也不补造统计值。
- NAGA 缺失时仍从天凤牌谱计算非 Rating 指标。
- 缺失数据保持缺失状态，不补造统计值。
